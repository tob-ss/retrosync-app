# GraphQL schema example
#
# https://gqlgen.com/getting-started/

#type Metadata {
#  id: Int!
#  deviceID: String!
#  lid: String!
#  gameID: String!
#  gameName: String!
#  lastMod: Int!
#  hash: String!
#}

#type Query {
#  GetAllSaves [Metadata!]!
#  GetOneSave(id: Int!): Metadata!
#}


type Post {
  Device: String!
  Console: String!
  Directories: [String!]
  Timemod: [Int!]
}

type UseID {
  ID: Int!
}

type PostTime {
  ID: Int!
  TimeMod: Int!
}

type DeviceName {
  Device: String!
}

type Paths {
  IDs: [Int!]
  Paths: [String!]
}

input InputID {
  ID: Int!
}

input UpdateTime {
  ID: Int!
  TimeMod: Int!
}

input PostDeviceName {
  Device: String!
}

input PostSaves {
  Device: String!
  Console: String!
  Directories: [String!]
  Timemod: [Int!]
}

type Mutation {
  createSaves(input: PostSaves!): Post!
  deleteLocal(input: PostDeviceName!): DeviceName!
  updateTime(input: UpdateTime!): PostTime!
  deleteLocalID(input: InputID!): UseID!
}

type Query {
  getPaths(Device: String!, UserID: Int!): Paths!
}